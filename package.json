{
  "name": "jianyi",
  "private": true,
  "scripts": {
    "build:export": "esbuild js/export-mux.js --bundle --minify --format=iife --platform=browser --outfile=vendor/mp4-mov.js"
  },
  "devDependencies": {
    "mediabunny": "1.55.7",
    "esbuild": "0.28.2"
  },
  "license": "SEE LICENSE IN LICENSE"
}
